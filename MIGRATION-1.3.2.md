# Migración 1.3.1 -> 1.3.2

No requiere cambios de configuración.

Este parche corrige el consumo/desaparición de loot nodes sin recompensas válidas.
Los nodos con `entries: {}` ahora permanecen en el mundo hasta que configures su tabla desde `/mdvheadores loot editor <id>`.
